<%= form_for(@photo) do |f| %>
  <% if @photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% @photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <div class="row">
      <div class="col-xs-12 col-sm-6 col-sm-offset-3 col-md-6 col-md-offset-3 col-lg-6 col-lg-offset-3">
        <div id="logbox">
          <form id="signup" method="post" action="/signup">
            <div class="field" style="padding-top:10px;">
              <%= f.file_field :image, :class => "input pass" %>
            </div>
            <div class="field">
              <%= f.text_area :caption, :class => "input pass", :placeholder => "作品名を入力してください", :maxlength => "30" %>
              <%= f.text_area :description, :class => "input pass",:placeholder => "作品の説明を入力してください", :size => "20x10" %>
              <%= f.hidden_field :user_id, :class => "input pass" %>
            </div>
            <div class="actions">
              <%= f.submit "画像を投稿する", :class => "inputButton" %>
            </div>
          </form>
        </div>
      </div>
    </div>



<% end %>
